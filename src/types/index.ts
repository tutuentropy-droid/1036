export interface Movie {
  id: string;
  title: string;
  originalTitle?: string;
  year: number;
  director: string;
  country: string;
  genre: string[];
  posterColor: string;
  posterGradient?: string;
 错位Fact: string;
 错位Director?: string;
  emoji: string;
}
